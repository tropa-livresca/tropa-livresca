/**
 * Botão de edição (aparecer menu para edição e imprimir todos os dados vinculados)
 * Formato do livro 
 * Detalhes
 * Conteúdo (Incluir pré-visualização da capa e do documento)
 * Orçamento
 * Botões de salvar como  rascunho e publicar
 */

import { useState, useEffect } from "react";
import Input from "../../../../components/form/Input/Input";

export default function Confirmacao() {
  return (
    <main>
      <form>
      </form>
    </main>
  );
}
